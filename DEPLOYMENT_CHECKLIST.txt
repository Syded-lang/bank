═══════════════════════════════════════════════════════════════
🚀 DEPLOYMENT CHECKLIST FOR HOSTINGER
═══════════════════════════════════════════════════════════════

## PRE-DEPLOYMENT STATUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Local testing complete
✅ Database configured (bank database with 55 tables)
✅ License bypass implemented (6 files)
✅ Installer fixed (permission check + .env path)
✅ XAMPP MySQL socket configured
✅ All caches cleared
✅ Project cleaned


## DEPLOYMENT STEPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### STEP 1: Push to GitHub
□ Add all changes to git
□ Commit with message
□ Push to main branch

### STEP 2: Connect to Hostinger via SSH
```bash
ssh u299375718@eastbridgeatlantic.com -p 65002
```
Password: Fishpoder123

### STEP 3: Backup Current Files (if any)
```bash
cd ~/public_html
mv core core_backup_$(date +%Y%m%d) 2>/dev/null || true
```

### STEP 4: Clone from GitHub
```bash
cd ~/public_html
git clone https://github.com/Syded-lang/bank.git temp_repo
mv temp_repo/core .
mv temp_repo/.htaccess .
mv temp_repo/index.php .
rm -rf temp_repo
```

### STEP 5: Set Correct Permissions
```bash
cd ~/public_html/core
chmod -R 755 .
chmod -R 777 storage
chmod -R 777 bootstrap/cache
```

### STEP 6: Create .env File
```bash
cd ~/public_html/core
cat > .env << 'ENVFILE'
APP_NAME=ViserBank
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://eastbridgeatlantic.com

LOG_CHANNEL=stack

DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=u299375718_db
DB_USERNAME=u299375718_user
DB_PASSWORD=Fishpoder123

BROADCAST_DRIVER=log
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null

PURCHASECODE=BYPASSED-LICENSE-PRODUCTION
ENVFILE
```

### STEP 7: Generate Application Key
```bash
cd ~/public_html/core
php artisan key:generate
```

### STEP 8: Import Database
Option A - SSH with mysql command:
```bash
mysql -h localhost -u u299375718_user -p u299375718_db < ~/public_html/install/database.sql
# Enter password: Fishpoder123
```

Option B - phpMyAdmin (RECOMMENDED):
1. Go to: https://eastbridgeatlantic.com:2083
2. Login with cPanel credentials
3. Open phpMyAdmin
4. Select database: u299375718_db
5. Click "Import" tab
6. Upload: install/database.sql
7. Click "Go"

### STEP 9: Clear All Caches
```bash
cd ~/public_html/core
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
```

### STEP 10: Test the Application
□ Visit: https://eastbridgeatlantic.com
□ Should see ViserBank homepage
□ Test admin login: https://eastbridgeatlantic.com/admin


## HOSTINGER CREDENTIALS SUMMARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SSH Access:
- Host: eastbridgeatlantic.com
- Port: 65002
- Username: u299375718
- Password: Fishpoder123

Database:
- Host: localhost
- Database: u299375718_db
- Username: u299375718_user
- Password: Fishpoder123

cPanel:
- URL: https://eastbridgeatlantic.com:2083
- Username: u299375718
- Password: Fishpoder123

File Manager:
- URL: https://eastbridgeatlantic.com:2083/cpsess.../frontend/jupiter/filemanager/


## FILE STRUCTURE ON HOSTINGER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

~/public_html/
├── .htaccess                    # Routing to core/public
├── index.php                    # Bootstrap file
└── core/                        # Laravel application
    ├── .env                     # Created manually (Step 6)
    ├── app/
    ├── public/                  # Web root
    │   └── index.php
    ├── storage/                 # 777 permissions
    ├── bootstrap/cache/         # 777 permissions
    └── vendor/


## TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### Error 500 after deployment:
```bash
cd ~/public_html/core
php artisan config:clear
php artisan cache:clear
tail -50 storage/logs/laravel.log
```

### Database connection error:
- Verify .env has correct credentials
- Test: mysql -h localhost -u u299375718_user -p
- Check if database exists in phpMyAdmin

### Assets not loading (404 errors):
- Check .htaccess file is in public_html root
- Verify RewriteEngine On
- Check file permissions (755 for directories)

### White screen / blank page:
```bash
cd ~/public_html/core
chmod -R 777 storage
chmod -R 777 bootstrap/cache
php artisan config:clear
```


## ADMIN PASSWORD RESET (if needed)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Via SSH:
```bash
cd ~/public_html/core
php artisan tinker

# In tinker:
$admin = App\Models\Admin::first();
$admin->password = bcrypt('admin123');
$admin->save();
exit
```

Via phpMyAdmin:
```sql
UPDATE admins 
SET password = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi'
WHERE id = 1;
-- Password will be: password
```


## POST-DEPLOYMENT CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

□ Homepage loads correctly
□ Admin panel accessible
□ Admin can login
□ CSS/JS files loading
□ Images displaying
□ Database queries working
□ No 500 errors
□ License bypass active (no activation screen)
□ All routes working


## MAINTENANCE MODE (if needed)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Put site in maintenance:
```bash
cd ~/public_html/core
php artisan down --secret="update-in-progress"
```

Access while in maintenance:
https://eastbridgeatlantic.com/update-in-progress

Bring site back up:
```bash
php artisan up
```


═══════════════════════════════════════════════════════════════
🎯 READY TO DEPLOY!
═══════════════════════════════════════════════════════════════

Next commands to run:
1. Git add, commit, push (see below)
2. SSH to Hostinger and follow steps 1-10 above

═══════════════════════════════════════════════════════════════
